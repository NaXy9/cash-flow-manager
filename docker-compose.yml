services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - sqlite_data:/app/db
    environment:
      - DEBUG=True
      - SECRET_KEY=dev-secret-change-in-production
      - ALLOWED_HOSTS=localhost 127.0.0.1 0.0.0.0

volumes:
  sqlite_data:
